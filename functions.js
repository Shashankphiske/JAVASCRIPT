function cart(...num1){ // this will get as many arguments as you pass and return an array (it is a rest operator)
    console.log(num1); // the same rest operator is spread operator that we use
} 
cart(100,200,300);