var data = 33;

console.log(data);

// now if the data was a string of 33 : "33" then we will convert it into number using Number operator
// var newData = Number(data);

// if we convert "33abc" it will input NaN (not a number) inside the variable and its data type will be a number
// if we convert a pure string the same as above
// if we convert a null value, it will return 0
// if we convert a undefined it will be undefined
// if we convert a boolean value then 1 for true and 0 for false

// we can convert into boolean too using the Boolean operator
// if the number is 1 then true and number is 0 then false
// if a string has a character or multiple characters in it then true and false if none is present like : ""

// same we have a operator String

//OPERATIONS :
let value = 3
let negValue = -value; //-3
// ** power
// / divide
// % remainder
// when we add 
// postix ++
// ++ prefix